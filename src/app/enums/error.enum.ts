export enum ErrorMessage {
  Banner = 'Error fetching banner data:',
  home = 'Error fetching home card data:',
  category = 'Error fetching category card data:',
  genre = 'Error fetching genre card data:',
  search = 'Error fetching genre card data:',
  detail = 'Error fetching movie detail data:',
  video = 'Error fetching movie video data:',
  genrelist = 'Error fetching movie genres:'
}
